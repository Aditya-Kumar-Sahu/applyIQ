export default function HomePage() {
  return (
    <main className="page-shell">
      <section className="hero-card">
        <p className="eyebrow">ApplyIQ</p>
        <h1>Your job search, on autopilot.</h1>
        <p className="lede">
          Phase 1 foundation is in place. Next we will layer in auth, resume intelligence, and the
          orchestration pipeline.
        </p>
      </section>
    </main>
  );
}
