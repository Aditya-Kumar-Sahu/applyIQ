global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'applyiq-backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['backend:8000']
    headers:
      X-Metrics-Secret: "applyiq-metrics-secret"
